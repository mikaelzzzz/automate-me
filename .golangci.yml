version: "2"

linters:
  default: standard
  enable:
    - errorlint
    - copyloopvar
    - nilerr
    - unconvert
    - misspell
  settings:
    errcheck:
      check-type-assertions: true

formatters:
  enable:
    - gofmt
    - goimports
